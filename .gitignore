# Per-target analysis outputs -- these are generated data, not source, and
# will grow large as we run more targets. Kept locally, not pushed.
TICs/
Data/Atlas/
Data/ztf/

# Local junk
.DS_Store
__pycache__/
*.pyc
.ipynb_checkpoints/

# Per-run batch state/logs/results -- regenerated each run, not source
atlas_batch_state.json
batch_atlas_fetch*.log
batch_score*.log
*_score_results.json
